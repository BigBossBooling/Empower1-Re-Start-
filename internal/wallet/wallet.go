package wallet

// Package wallet contains the core logic for EmPower1 wallets.
// This includes key generation, address management, transaction construction
// and signing, UTXO selection, and interaction with the node via RPC.
// It serves as a backend for CLI and GUI wallets.
