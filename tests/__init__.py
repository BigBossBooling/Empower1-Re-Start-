# This file makes Python treat the `tests` directory as a package.
# It can be empty.

# You can also put global test configurations or fixtures here if needed,
# though conftest.py is more common for fixtures.

print("Test package initialized.")
